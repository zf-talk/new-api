upstream new_api_upstream {
    server 127.0.0.1:13000;
    keepalive 32;
}

upstream bepusdt_upstream {
    server 127.0.0.1:13001;
    keepalive 16;
}

server {
    listen 80;
    server_name api.example.com;

    access_log /var/log/nginx/new-api-access.log;
    error_log /var/log/nginx/new-api-error.log warn;

    client_max_body_size 50m;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_pass http://new_api_upstream;
    }
}

server {
    listen 80;
    server_name pay.example.com;

    access_log /var/log/nginx/pay-access.log;
    error_log /var/log/nginx/pay-error.log warn;

    client_max_body_size 20m;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
        proxy_pass http://bepusdt_upstream;
    }
}
